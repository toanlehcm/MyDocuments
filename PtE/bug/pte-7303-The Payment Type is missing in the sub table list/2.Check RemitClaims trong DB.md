Edited v2_Report.js
Viewed v4_RemitClaims.js:1-125

### ✅ XÁC NHẬN: RemitClaims Mapping

Chính xác anh ạ. `RemitClaimsSchema` trong file [v4_RemitClaims.js](file:///d:/Sources/pteverywhere/Server/models/v4_RemitClaims.js) đang trỏ trực tiếp đến collection **`RemitClaims`** trong database.

Dưới đây là các minh chứng kỹ thuật:

1.  **Chỉ định Collection tường minh:** Tại dòng **106**, Schema option đã được set cứng:
    ```javascript
    106:   { collection: "RemitClaims" }
    ```
    Trong Mongoose, khi có option này, nó sẽ ghi đè quy tắc đặt tên mặc định và trỏ thẳng vào collection được chỉ định (có phân biệt hoa/thường).

2.  **Khớp cấu trúc dữ liệu:** So sánh giữa file code và hình ảnh database anh gửi:
    *   **Code:** Có các trường `RemitClaimNo`, `ClaimPayment`, `PatientResp`, `PayerName`, `PayerCode`, `StatusCode`, `Adjustments` (Array), `Services` (Array).
    *   **DB (Hình ảnh):** Các trường này xuất hiện 100% khớp về cả tên và kiểu dữ liệu.

---

### 🔍 Liên quan đến việc tìm field "Payment Type":

Nhìn vào hình ảnh database anh gửi và cấu trúc Schema:
- **Root Document:** Không có trường `PaymentType` hay `Type` ở cấp cao nhất của collection `RemitClaims`. 
- **Nguồn dữ liệu:** Dựa vào Pipeline trong `v2_ReportDAL.js` mà em đã xem trước đó, trường `Type` thường nằm trong collection **`InsuranceERA`** (được `$lookup` vào qua trường `ERA`).

**Gợi ý bước tiếp theo:** 
Nếu anh muốn kiểm tra field `Type` thực sự nằm ở đâu, anh có thể soi document trong collection **`InsuranceERA`** bằng cái `_id` lấy từ trường `ERA` ở document trên hình (ví dụ: `61246885eb187527ead30757`).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📍 **Em đã sẵn sàng nếu anh cần bóc tách dữ liệu từ InsuranceERA sang báo cáo.**
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ Soi thử collection InsuranceERA?
2️⃣ Update Pipeline để lấy Type từ ERA?
3️⃣ Làm việc khác?