# Workflow biến `appointmentList` — Từ khởi tạo đến Client

> Trace từ code thực tế. Không đoán mò.

---

## 🗺️ Sơ đồ tổng quan

```
[Client gửi request tạo lịch]
         │
         ▼
┌─────────────────────────────────────────────────────┐
│ ACTION: exports.v4CreateAppointmentPtE              │
│ File: Server/actions/v2_Appointment.js              │
│ Nhiệm vụ: nhận params, gọi initializer             │
└────────────────────┬────────────────────────────────┘
                     │ gọi
                     ▼
┌─────────────────────────────────────────────────────┐
│ INITIALIZER: createAppointmentDataWithValidationPtE │
│ File: Server/initializers/v2_appointment.js         │
│ Nhiệm vụ: bridge, gọi logic handler                │
└────────────────────┬────────────────────────────────┘
                     │ gọi
                     ▼
┌─────────────────────────────────────────────────────┐
│ LOGIC: bookAppointmentHanlderPtE                    │
│ File: Server/logicMongo/v2_AppointmentLogic.js      │
│ Dòng: 14179                                         │
│ Nhiệm vụ: orchestrate toàn bộ luồng booking        │
│                                                     │
│  Line 14214: var appointmentList = [];  ← KHÔNG DÙNG│
│             (chỉ declare, KHÔNG bao giờ được push)  │
│                                                     │
│  Bước 1 ──► _initialBookingDataPtE() [L.14413]     │
│  Bước 2 ──► _validateBookingDataPtE() [L.14424]    │
│  Bước 3 ──► _startTransactionBooking...() [L.14581] │
└─────────────────────────────────────────────────────┘
```

---

## 📦 BƯỚC 1: Sinh ra `appointmentList`

**Hàm:** `appointmentUtil.initAppointmentData()`
**File:** `Server/logicMongo/utils/AppointmentUtil.js`
**Dòng:** 845

```
initAppointmentData(clientDoc, appointment, userId, role, next)
   │
   │  Line 846: var appointmentList = []   ← KHỞI TẠO RỖng
   │
   ├─── [CASE A] isRepeat == false (appointment bình thường)
   │       Line 905: appointmentList.push(appointment)
   │       → Mảng chỉ có 1 phần tử
   │
   └─── [CASE B] isRepeat == true (appointment lặp)
           Loop qua weekDay × repeatNumber (Daily/Weekly/Monthly)
           Line 1075: appointmentList.push(newAppointment)
           → Mảng có N phần tử (1 phần tử / 1 lịch lặp)
           
   Cuối hàm:
   Line 1085: bookingData.appointmentList = []
   Line 1088: bookingData.appointmentList = appointmentList  ← GÁN
   Line 1097: next(null, bookingData)
```

---

## 📦 BƯỚC 2: Truyền qua `bookingData`

**Hàm:** `_initialBookingDataPtE()`
**File:** `Server/logicMongo/v2_AppointmentLogic.js`
**Dòng:** 17851

```
_initialBookingDataPtE(...)
   │
   │  Gọi appointmentUtil.initAppointmentData(...)
   │     → nhận lại res = bookingData
   │
   Line 17867: bookingData.appointmentListData = res.appointmentList
   │                                             ↑ GÁN từ initAppointmentData
   Line 17929: next(null, bookingData)
   
bookAppointmentHanlderPtE nhận lại:
   Line 14417: bookingData = res
   (bookingData.appointmentListData = mảng các lịch lặp)
```

---

## 📦 BƯỚC 3: Validate và tạo `dataOverlap`

**Hàm:** `_validateBookingDataPtE()`
**File:** `Server/logicMongo/v2_AppointmentLogic.js`
**Dòng:** 17934

```
_validateBookingDataPtE(bookingDoc, bookingData, appointment, ...)
   │
   Line 17951: var appointmentList = bookingData.appointmentListData || []
   │                                 ↑ ĐỌC từ bookingData
   Line 17952: var appointmentValidList = bookingData.appointmentListData || []
   │
   Line 17957: var responseBody = { dataOverlap: {}, appointmentValidList, ... }
   │
   ├─── [CASE 1] isRepeat && !confirmAcceptRepeatAppointment
   │       Line 18142: _validateRepeatedAppointments(
   │                       clientId, appointment, appointmentList, ...)
   │                                               ↑ TRUYỀN appointmentList vào
   │       callback:
   │         nếu err  → Line 18144: validateObject = res  (error path)
   │         nếu ok   → Line 18147: responseBody.dataOverlap = res  ← GÁN
   │
   └─── [CASE 2] confirmAcceptRepeatAppointment == true
           Line 18154: appointmentValidList = _.cloneDeep(appointment.appointmentValidList)
           Line 18160: _validateRepeatedAppointments(
                           clientId, appointment, appointmentValidListFilter, ...)
                           → nếu err: Line 18163: responseBody.dataOverlap = res
   
   Cuối hàm:
   Line 18267: next(err, responseBody)
   (responseBody.dataOverlap = kết quả từ _validateRepeatedAppointments)
```

---

## 📦 BƯỚC 4: `appointmentList` được gán vào `dataOverlap.content`

**Hàm:** `_validateRepeatedAppointments()`
**File:** `Server/logicMongo/v2_AppointmentLogic.js`
**Dòng:** 10425

```
_validateRepeatedAppointments(clientId, appointment, appointmentList, ...)
   │
   Line 10429: var appointmentListClone = _.cloneDeep(appointmentList)
   │                                      ↑ CLONE toàn bộ mảng
   Line 10434: var appointmentValidList = appointmentList || []
   Line 10435: var unavailableAppointmentResponseList = []
   │
   │ Chạy 4 sub-validation song song (async.waterfall):
   │   ① _validateAppointmentListConflictClientTime()    → tìm conflict BreakTime/TimeOff
   │   ② _validateConflictOtherAppointments()            → tìm conflict Appointment khác
   │   ③ _validateConflictGoogleCalendarEvent()          → tìm conflict Google Calendar
   │   ④ _validateAppointmentListConflictUnavailableTime()→ tìm conflict Unavailable block
   │      (tất cả kết quả concat vào unavailableAppointmentResponseList)
   │
   └─── Bước cuối: xây dựng responseBody.data.content
   
   ┌─── [CASE A] unavailableAppointmentResponseList.length > 0 (CÓ conflict)
   │     Lines 10646-10653:
   │       responseBody.data.code = APPOINMENT_OVERLAP_UNAVAILABLE
   │       responseBody.data.content = {
   │           unavailableAppointmentList: unavailableAppointmentResponseList,
   │           appointmentList: appointmentListClone,   ← GÁN Line 10649
   │           appointmentValidList
   │       }
   │       callback(errorCode.APPOINMENT_OVERLAP_UNAVAILABLE)  ← ERROR path
   │
   └─── [CASE B] không có conflict, nhưng isRepeat && !confirmAccept
         Lines 10656-10669:
           responseBody.data.code = APPOINMENT_OVERLAP_UNAVAILABLE
           responseBody.data.content = {
               unavailableAppointmentList: [],  (rỗng)
               appointmentList: appointmentListClone,   ← GÁN Line 10668
               appointmentValidList
           }
           callback(null, responseBody.data)             ← SUCCESS path có overlap data
   
   Kết quả trả về qua next():
     Line 10680: next(err, responseBody.data)   → khi có conflict
     Line 10682: next(err, result)              → success, result = responseBody.data
```

---

## 📦 BƯỚC 5: `dataOverlap` bubble up qua các tầng

**Hàm:** `bookAppointmentHanlderPtE` (tiếp theo)
**Dòng:** 14424–14451

```
_validateBookingDataPtE callback:
   validateObject = responseBody từ _validateBookingDataPtE
                  = { dataOverlap, appointment, appointmentValidList }
   validateObject.dataOverlap = kết quả từ _validateRepeatedAppointments
                               = { code, content: { appointmentList, ... } }

   Line 14444: if (validateObject.dataOverlap && !_confirmAcceptRepeatAppointment)
                 if (validateObject.dataOverlap.code == APPOINMENT_OVERLAP_UNAVAILABLE)
   Line 14446:     responseBody.dataOverlap = validateObject.dataOverlap  ← GÁN, return error
   
   Line 14449: else
   Line 14450:   responseBody.dataOverlap = validateObject.dataOverlap  ← GÁN, tiếp tục

   Cuối waterfall (Line 14720-14748):
     Line 14733: if (responseBody.dataOverlap.code == APPOINMENT_OVERLAP_UNAVAILABLE)
     Line 14734:   return next(null, null, responseBody)  ← trả về có dataOverlap
     
     Line 14748: next(null, null, responseBody.appointmentListResult)  ← trả về bình thường
```

---

## 📦 BƯỚC 6: Qua Initializer → Action → Client

**Initializer:** `createAppointmentDataWithValidationPtE`
**File:** `Server/initializers/v2_appointment.js` ~ dòng 511

```
nhận data từ bookAppointmentHanlderPtE
   → data.response = { data: responseBody }
   → data.response.data.dataOverlap.content.appointmentList  ← đây là đích cuối cùng
```

**Action:** `v4CreateAppointmentPtE`
**File:** `Server/actions/v2_Appointment.js` dòng 3606

```javascript
console.log(data.response.data.dataOverlap.content.appointmentList);
// ↑ console.log đã thêm để debug
```

---

## 📊 BẢNG: Các case `appointmentList` được gán value

| # | Dòng | File | Hàm | Value gán | Ghi chú |
|---|------|------|-----|-----------|---------|
| **1** | **846** | `AppointmentUtil.js` | `initAppointmentData` | `= []` | Khởi tạo rỗng |
| **2** | **905** | `AppointmentUtil.js` | `initAppointmentData` | `.push(appointment)` | Nếu KHÔNG repeat → push 1 lịch |
| **3** | **1075** | `AppointmentUtil.js` | `initAppointmentData` | `.push(newAppointment)` | Nếu CÓ repeat → push N lịch vào loop |
| **4** | **17851** | `v2_AppointmentLogic.js` | `_initialBookingDataPtE` | `bookingData.appointmentListData = res.appointmentList` | Bridge từ util vào bookingData |
| **5** | **17951** | `v2_AppointmentLogic.js` | `_validateBookingDataPtE` | `= bookingData.appointmentListData \|\| []` | Read lại để validate |
| **6** | **10429** | `v2_AppointmentLogic.js` | `_validateRepeatedAppointments` | `appointmentListClone = _.cloneDeep(appointmentList)` | Clone trước khi dùng |
| **7** | **10649** | `v2_AppointmentLogic.js` | `_validateRepeatedAppointments` | `content.appointmentList = appointmentListClone` | **Gán vào dataOverlap.content — CÓ CONFLICT** |
| **8** | **10668** | `v2_AppointmentLogic.js` | `_validateRepeatedAppointments` | `content.appointmentList = appointmentListClone` | **Gán vào dataOverlap.content — KHÔNG CONFLICT** |
| **9** | **18154** | `v2_AppointmentLogic.js` | `_validateBookingDataPtE` | `appointmentValidList = _.cloneDeep(appointment.appointmentValidList)` | Re-validate khi user confirm rồi |

---

## 🎯 Điểm gán QUAN TRỌNG NHẤT để debug

`data.response.data.dataOverlap.content.appointmentList` đến từ:

> **`AppointmentUtil.js` dòng 10649 hoặc 10668** — bên trong `_validateRepeatedAppointments()`
> Giá trị = **`appointmentListClone`** = `_.cloneDeep(appointmentList)` từ dòng 10429
> Giá trị gốc = `appointmentList` được build trong `AppointmentUtil.initAppointmentData()` (dòng 846–1088)

### Vì sao cần know điều này?

Nếu `appointmentList` trong response **sai** (thiếu/thừa phần tử, sai ngày):
- **Bug nguồn gốc**: Nằm trong logic sinh lịch lặp tại `AppointmentUtil.js` dòng **997–1080** (vòng while-for tính `weekDay`)
- **Bug filter**: Nằm trong `_validateRepeatedAppointments` dòng **10636–10676** (filter `appointmentValidListFilter`)

---

*Trace completed từ source code. Không đoán mò.*
