Viewed v2_Client.js:1201-2000
Viewed v2_ClientLogic.js:2980-3779

Tôi đã có đủ thông tin. Đây là kết quả phân tích:

---

## ✅ Kết luận: Server PASS-THROUGH hoàn toàn

Trace đầy đủ flow **Client → Action → Logic → DAL → MongoDB**:

### Layer 1: `v3SaveLocationOfClinic` Action (`v2_Client.js` line 1581–1607)
```js
// Nhận locationData từ client params, gọi thẳng logic:
api.v2_client.saveLocationOfClinic(
    data.TokenUserInfo.Client,
    data.connection.params.locationData,  // ← pass-through, không transform gì
    ...
)
```
**Không có bất kỳ xử lý nào với `AddressDetails`.**

### Layer 2: `saveLocationOfClinic` Logic (`v2_ClientLogic.js` line 3329–3401)
```js
// Chỉ xử lý: MerchantData, DeviceCardReaders, relationshipList
// Sau đó gọi thẳng DAL với locationData nguyên vẹn:
await clinicLocationDAL.updateLocationOfClinicAsync(locationData, session)
```
**Không có transform nào với `AddressDetails.State` hay `AddressDetails.StateId`.**

### Layer 3: `updateLocationOfClinicAsync` DAL (`v2_ClinicLocationDAL.js` line 292–297)
```js
return clinicLocationModel.findOneAndUpdate(
    conditions,    // { _id: locationData._id }
    locationData,  // ← toàn bộ object, ghi thẳng vào DB
    {}
)
```
**`findOneAndUpdate` với `locationData` trực tiếp làm update document — whatever client gửi, MongoDB lưu y như vậy.**

---

### Kết luận cuối cùng

```
Client payload → Server → MongoDB
(không có transform)    (lưu nguyên xi)
```

> ✅ **Fix của chúng ta ở client-side (reverse-lookup StateId trước khi gửi) là đúng chỗ và đủ.**  
> Server chỉ là "hộp thư chuyển phát" — data đến như thế nào, vào DB như thế ấy. Không cần thêm gì ở server.