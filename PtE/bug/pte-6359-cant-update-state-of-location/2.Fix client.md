Edited locationManagementATP.js
Viewed locationManagementATP.js:1003-1041

Nhìn screenshot, tôi thấy rõ payload có `State: "CA"` nhưng `StateId` vẫn là stale ObjectId cũ từ DB. Cần reverse-lookup để sync cả hai trước khi gửi lên server.

Tôi sẽ thay thế đoạn comment ở lines 1033-1037 bằng logic sync thực sự:

Viewed locationManagementATP.js:1029-1045
Edited locationManagementATP.js

Verify kết quả:

Viewed locationManagementATP.js:1029-1063

✅ Code sạch, đúng chỗ.

---

## ✅ Fix hoàn tất

Thay vì chỉ để comment, bây giờ đoạn code thực hiện **reverse-lookup** trước khi gửi payload:

```js
// Trước khi gửi API:
// Country text "USA" → tìm trong $scope.countries → lấy CountryId thực
// State text "California" → tìm trong $scope.states → lấy StateId thực
```

### Behavior sau fix:

| Scenario | State | StateId |
|---|---|---|
| User chọn "California" từ dropdown | `"California"` | ObjectId của California ✅ |
| User gõ tự do "New York" (không có trong list) | `"New York"` | `null` ✅ |
| Country không phải US/AU (`$scope.states` rỗng) | `""` | `null` ✅ |
| User không đổi gì (State giữ nguyên "AK") | `"AK"` | `null` nếu "AK" không match Name trong states list |

> ⚠️ **Lưu ý edge case:** Nếu DB lưu state là abbreviation `"AK"` (cũ) thay vì full name `"Alaska"` — sau khi user **không đổi gì** và save, `StateId` sẽ thành `null` vì `"AK"` không match `state.Name`. Đây là acceptable vì data cũ đã không nhất quán. Chỉ cần user chọn lại state từ dropdown lần này thì data sẽ đồng nhất về sau.