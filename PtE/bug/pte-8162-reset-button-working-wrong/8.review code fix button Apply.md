Edited insuranceClaimManagementATP.html
Edited insuranceInvoiceATP.html
Edited invoicePtE.js
Edited invoicePtE.js
Viewed invoicePtE.js:1380-1410

Để em đọc toàn bộ function `_getInvoices` hiện tại:

Viewed invoicePtE.js:1385-1495

Đọc thêm context `_parseDataForView` và `_makeRequestData` để review đầy đủ:

Searched for "function _parseDataForView"
Searched for "function _makeRequestData"
Searched for "function _invalidToSentRequest"
Searched for "_parseDataForView"
Searched for "common.applyChanges"
Viewed invoicePtE.js:1269-1295

Đã đủ thông tin. Viết báo cáo review:

---

# 🏥 CODE REVIEW: `_getInvoices` — Pre-Merge

## ✅ Điểm tốt (Không cần sửa)

| # | Điểm | Lý do |
|---|------|-------|
| 1 | **Guard clause đầu hàm** (line 1392) | `_invalidToSentRequest()` chặn sớm, không có side effect |
| 2 | **Null-safe default params** (line 1395-1400) | `pageIndex` và `pageSize` có fallback rõ ràng |
| 3 | **Null-check `_res.data`** (line 1418) | `if (_res && _res.data)` đúng pattern |
| 4 | **`.finally()` cho spinner** (line 1489-1491) | Spinner **luôn tắt** dù success hay error — fix đúng bug cũ |
| 5 | **`common.applyChanges($scope)`** (line 1491) | Đúng PTE rule: thay `$scope.$apply()` trực tiếp |
| 6 | **`$timeout` thay `setTimeout`** (line 1465-1477) | Đúng PTE `skill-ui-freeze-prevention.md` |
| 7 | **Cache comparison pattern** (line 1455-1481) | Giống Claims — consistent, proven |
| 8 | **`(cacheHeaderColumnVisible \|\| [])` guard** (line 1456) | Null-safe cho lần đầu load khi cache chưa có |

---

## ⚠️ Vấn đề cần xem xét

### 🔴 Issue #1 — `_res.data.docs` không có null guard

```javascript
// Line 1433 — HIỆN TẠI:
$scope.invoiceTable.invoices = _parseDataForView(_res.data.docs);
```

**Rủi ro:** Nếu server trả `_res.data` nhưng không có `docs` (ví dụ: trả `{ total: 0 }` khi lỗi bất thường), thì `_parseDataForView(undefined)` được gọi.

**Tuy nhiên**: `_parseDataForView` đã có guard ở line 1270: `if (!invoices || !invoices.length) return []` → **tự xử lý `undefined` an toàn**. Không crash.

> ✅ **Kết luận: Không cần sửa** — đã được bảo vệ bởi callee.

---

### 🟡 Issue #2 — `isChangeDisplayColumns` — side effect ngoài ý muốn

```javascript
// Line 393 — closure variable:
let isChangeDisplayColumns = false;

// Line 1455 — trong _getInvoices:
isChangeDisplayColumns = false;   // ← reset closure var, không phải local var
```

**Vấn đề**: Anh đã đổi từ `let isChangeDisplayColumns = false` (local) → `isChangeDisplayColumns = false` (ghi thẳng vào closure variable ở line 393). Đây là **behavioral change** so với Claims:

| | Claims | Invoice (hiện tại) |
|---|---|---|
| `let calculateTableSize = false, isChangeDisplayColumns = false` | Local — không ảnh hưởng closure | ❌ Không có `let` → ghi vào closure |

**Hệ quả thực tế**: Closure `isChangeDisplayColumns` (line 393) vốn được `$watch` ở line 495-499 set `true`/`false` khi user thay đổi columns trong filter panel. Nếu `_getInvoices` **reset nó về `false`** trước khi `$watch` có cơ hội chạy, sẽ không có vấn đề gì vì cache comparison (`columnBefore !== columnAfter`) bây giờ là **source of truth** thay thế.

> ✅ **Kết luận: An toàn về mặt logic** — `isChangeDisplayColumns` closure vẫn được `$watch` set, nhưng trong `_getInvoices` em dùng cache comparison để detect thay vì phụ thuộc vào closure var. Reset `= false` ở đây về cơ bản là vô hại. **Tuy nhiên**, nếu muốn align chính xác với Claims pattern (dùng local var), nên thêm lại `let`:

```javascript
// Đề xuất — align chính xác với Claims:
let isChangeDisplayColumns = false;  // local, shadow closure var
```

---

### 🟢 Issue #3 — `_res.data.page`, `.limit`, `.total` không có optional chaining

```javascript
// Line 1434-1436:
$scope.paging.pageIndex = _res.data.page;
$scope.paging.pageSize  = _res.data.limit;
$scope.paging.total     = _res.data.total;
```

**Rủi ro**: Nếu `_res.data.docs` tồn tại nhưng `page`/`limit`/`total` undefined → gán `undefined` vào scope, không crash nhưng UI hiển thị sai.

> ✅ **Kết luận: Pre-existing code**, không phải thay đổi mới. Nằm ngoài scope của PTE-8162. Có thể tạo ticket riêng nếu cần.

---

## 📋 Tóm tắt sức khỏe code

| Tiêu chí | Đánh giá |
|----------|----------|
| **Null safety** | ✅ Đủ cho scope PTE-8162 |
| **Error handling** | ✅ `.finally()` đảm bảo spinner luôn tắt |
| **PTE `skill-ui-freeze-prevention`** | ✅ `$timeout` đúng chuẩn |
| **PTE `anti-patterns.md #16`** | ✅ `common.applyChanges` thay vì `$scope.$apply()` trực tiếp |
| **PTE `patterns.md`** | ⚠️ `isChangeDisplayColumns` nên khai báo `let` để shadow closure |
| **Consistency với Claims** | ⚠️ Thiếu `let` ở line 1455 — không đồng nhất với Claims line 3522 |

---

## 🔧 1 dòng cần sửa trước merge

```diff
- isChangeDisplayColumns = false;
+ let isChangeDisplayColumns = false;
```

Chỉ thêm `let` — đúng pattern Claims, tránh side effect không mong muốn vào closure variable:

Edited invoicePtE.js

✅ Đã sửa. Function `_getInvoices` **sẵn sàng merge** với các điểm chính:

| | |
|---|---|
| **Null safety** | ✅ Pass |
| **PTE rules** | ✅ Pass |
| **Claims pattern** | ✅ Aligned |
| **Side effect closure** | ✅ Đã fix (`let` để shadow) |

Blank lines thừa ở line 1453-1454 không ảnh hưởng logic, có thể clean up nếu muốn nhưng không bắt buộc trước merge.