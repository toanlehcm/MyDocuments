**Root cause CONFIRMED 100%.**

```
[DBG-12] 🔴 waitQueue STUCK! Callback KHÔNG BAO GIỜ fire!
[DBG-12] → Root cause CONFIRMED: $$animateAsyncRun waitQueue bị kẹt từ page load
```

## Tổng kết root cause

**File:** angular.js — `$$AnimateAsyncRunFactoryProvider`

**Dòng gây lỗi:** Line 6253-6255 — flush loop **không có try/catch**:
```javascript
for (var i = 0; i < waitQueue.length; i++) {
    waitQueue[i]();  // ← 1 callback throw → loop dừng → queue kẹt vĩnh viễn
}
waitQueue = [];      // ← KHÔNG BAO GIỜ chạy
```

**Chuỗi sự kiện:**
1. F5 → new tab mở URL `/tasks/.../Appointment/...`
2. Double route change: `/tasks/.../Appointment/...` → `/tasks` (2x `TasksManagementCtrl`)
3. Route transition tạo animation callbacks → push vào `waitQueue`
4. `$$rAF` fires → flush loop bắt đầu chạy
5. **1 callback throw error** (do view route 1 bị destroy giữa chừng)
6. Loop dừng → `waitQueue = []` không chạy → **queue kẹt vĩnh viễn**
7. Click +Task → dialog animation `complete()` → `_tick()` push callback vào queue stuck → `length > 1` → không schedule `$$rAF` mới → **onComplete không bao giờ fire** → **freeze**

**Tại sao chờ lâu hơn cũng không giúp:** Queue stuck là **vĩnh viễn**, không tự recover. Chờ 5s hay 50s đều freeze (đúng như anh test — chờ 8s vẫn freeze ở case F5).

**Tại sao case không F5 thì OK:** Tab cũ đã có cache templates/state → route transition timing khác → callback không throw → queue healthy.

Khi anh sẵn sàng muốn fix, cho em biết.