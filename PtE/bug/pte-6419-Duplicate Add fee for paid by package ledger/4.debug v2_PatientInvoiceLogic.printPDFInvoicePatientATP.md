Edited paymentATP.html
Viewed v2_PatientInvoiceLogic.js:5064-5110

Em đã biết vị trí chính xác từ lần trace trước. Xác nhận lại nhanh:

Viewed v2_PatientInvoiceLogic.js:5745-5775

Đây là câu trả lời chính xác:

---

### Dòng code nhận value và render text trong PDF

**Điều kiện lọc — Line 5755:**
```javascript
if (invoiceData.Transactions[jindex].TransactionType == 'Upcharge' 
    || invoiceData.Transactions[jindex].Description == 'Add fee for paid by package') {
```
→ Đây là cái quyết định transaction nào được đưa vào HTML của PDF. Bất kỳ transaction nào có `Description == 'Add fee for paid by package'` đều bị render.

**Render text — Line 5759 + 5767:**
```javascript
// Line 5759: Lấy Description từ DB ra gán vào biến description
let description = txn.Description || '';

// Line 5767: Render text đó thành HTML cell trong PDF
'<td width="60%">' + description + '</td>'
```

**Render value (số tiền) — Line 5768:**
```javascript
'<td width="40%">' + formatCurrencyNumber(
    invoiceData.Transactions[jindex].NewRemainAmount 
    - invoiceData.Transactions[jindex].RemainAmount   // ← đây là value số tiền hiển thị
) + '</td>'
```

---

### Tóm tắt flow

```
DB.Transactions[]
  ↓ (read-only, không modify)
invoiceData.Transactions[jindex].Description  →  line 5759: gán vào biến description
invoiceData.Transactions[jindex].NewRemainAmount - RemainAmount  →  line 5768: tính số tiền
  ↓
Line 5766-5769: ghép thành HTML <tr><td>...</td></tr>
  ↓
Render vào file PDF
```

**Kết luận:** PDF chỉ **đọc và render trung thực** những gì có trong `invoiceData.Transactions` (lấy từ DB). Nếu trong DB có 2 dòng `Description = 'Add fee for paid by package'` thì PDF sẽ render đúng 2 dòng. **Bug nằm hoàn toàn ở khâu ghi dữ liệu vào DB**, không phải ở đây.