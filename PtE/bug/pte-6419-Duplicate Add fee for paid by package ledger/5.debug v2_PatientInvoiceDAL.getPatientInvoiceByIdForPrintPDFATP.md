Edited 4.debug%20server.md
Viewed v2_PatientInvoiceDAL.js:1574-1621

Để xác nhận, em xem tiếp hàm `getPatientInvoiceByIdForPrintPDFATP` xem nó select những field nào:

Viewed v2_PatientInvoiceDAL.js:1598-1720

**Câu trả lời: Đúng, nhưng quan trọng hơn là không có `.select()`.**

### Xác nhận

Hàm `getPatientInvoiceByIdForPrintPDFATP` (line 1598-1659):

```javascript
var query = patientInvoiceModel.findOne({ "_id": invoiceId })
    .populate(...)  // chỉ populate các ref fields (Patient, CreatedBy, Appointment...)
    // KHÔNG CÓ .select() → lấy TẤT CẢ fields của invoice document
    .lean().exec()
```

**Vì không có `.select()`** → hàm này trả về **toàn bộ document** từ MongoDB bao gồm `Transactions[]` với tất cả các transaction đã được lưu vào DB.

---

### Flow hoàn chỉnh từ DB → PDF

```
MongoDB: PatientInvoice.Transactions[]   ← data gốc lưu trong DB
         (có thể có 1 hoặc nhiều "Add fee..." tùy khi nào bị duplicate)
    ↓
DAL: getPatientInvoiceByIdForPrintPDFATP (line 1598)
     findOne({_id}).lean()  →  trả nguyên xi tất cả fields, KHÔNG filter Transactions
    ↓
Logic: printPDFInvoicePatientATP (line 5086)
     invoiceData = data trả về từ DAL
     invoiceData.Transactions = đúng mảng gốc từ DB
    ↓
Line 5754-5770: Loop qua từng Transactions[jindex]
     if (Description == 'Add fee for paid by package') → render 1 dòng trong PDF
     → Có 2 entry trong DB = render 2 dòng trong PDF
```

---

**Kết luận xác nhận 100%:** `invoiceData.Transactions` chính là array lấy trực tiếp từ MongoDB thông qua `getPatientInvoiceByIdForPrintPDFATP`. DAL không filter hay transform gì cả. **Bug duplicate hoàn toàn do DB đang chứa 2 entries** — fix phải ở tầng ghi dữ liệu.