Edited 1.Quick%20debug.md
Viewed v2_PatientInvoiceLogic.js:26241-26288

Bắt đầu fix. Đọc lại code hiện tại để apply chính xác.

Viewed v2_PatientInvoiceLogic.js:26306-26374

Code hiện tại rõ ràng. Apply Option A — thêm logic remove existing fee/promotion trước khi push.

Edited v2_PatientInvoiceLogic.js

Verify code đã apply đúng:

Viewed v2_PatientInvoiceLogic.js:26306-26400

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[ignoring loop detection]

✅ **PTE-6419 Fix đã apply xong.**

### Thay đổi

**File**: [v2_PatientInvoiceLogic.js](file:///d:/Sources/pteverywhere/Server/logicMongo/v2_PatientInvoiceLogic.js#L26335-L26396) — hàm `correctServiceChargesTransactions`

**Logic thêm** (line 26336-26360): Trước khi push fee/promotion mới, tìm và xóa existing transaction cùng loại:

1. `_.findIndex` tìm transaction có `IsApplyPackage + TransactionType='Other' + TransactionMethod='Package'` + matching Description
2. Nếu tìm thấy → **reverse amounts** (`Upcharge` hoặc `Discount`/`AppliedPackageData.Package`) 
3. `splice` xóa transaction cũ khỏi array
4. Recalculate `Balance` từ last remaining transaction
5. Sau đó logic push mới chạy bình thường → **luôn chỉ có 1 entry**

### Test thủ công theo steps trong ticket

Anh cần test lại theo flow:
1. Login CU → Packages → Create Package ($0.01, 2 sessions, $0.10)
2. Buy Package → Create Service Invoice → Apply Package
3. Edit Package Invoice → Refund → Apply lại
4. More > Preview → verify **chỉ 1 dòng** "Add fee for paid by package"

Cần em hỗ trợ gì thêm không anh?