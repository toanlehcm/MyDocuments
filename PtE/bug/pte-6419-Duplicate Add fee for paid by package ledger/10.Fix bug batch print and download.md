Viewed v2_PatientInvoiceLogic.js:6877-6911
Edited v2_PatientInvoiceLogic.js

Fix xong. Tóm tắt những gì đã thay đổi tại `transformInvoiceContentToTemplate` (line 7399–7425):

| # | Thay đổi | Lý do |
|---|----------|-------|
| **+** `idxRefundPkg` | `_.findLastIndex` tìm refund-package transaction cuối cùng | Guard chính — quyết định "Add fee" nào được hiện |
| **+** `isPackageFee` | Check `jindex > idxRefundPkg` trước khi render | Lọc bỏ các entry cũ trước refund |
| **+** `description` trim | Xóa `(X%)` khỏi Upcharge description khi `TransactionAmountType == 'P'` | Đồng bộ với PTE-3194 đã fix ở single-preview |
| **+** `txn.TransactionAmount != null` | Guard trước khi build regex | Tránh regex `/(null%)/` |
| **+** `(txn.NewRemainAmount \|\| 0) - (txn.RemainAmount \|\| 0)` | Fallback về 0 nếu field undefined | Tránh `NaN` trên PDF |

**Kết quả:** `transformInvoiceContentToTemplate` giờ dùng logic **y hệt** single-preview (`printPDFInvoicePatientATP` line 5755). Batch Print và Batch Download sẽ không còn hiển thị duplicate "Add fee" nữa.

**Manual test nhanh:**
1. Tìm invoice: trả bằng package → refund → trả lại
2. Batch Print → kiểm tra PDF → chỉ còn 1 dòng "Add fee"
3. So sánh với single-preview → kết quả phải khớp