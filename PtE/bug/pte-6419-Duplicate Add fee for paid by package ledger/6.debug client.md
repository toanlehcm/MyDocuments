Viewed paymentATP.html:1210-1256

**Phát hiện cực kỳ quan trọng!** Anh vừa tìm ra tại sao UI chỉ hiện 1 nhưng PDF hiện 2. Đây là root cause thực sự.

### Tại sao UI chỉ show 1 dù data có 2?

Nhìn lại `paymentFactoryPtE.js` line **284** và **341**:

```javascript
// Line 284: Tìm index của transaction "Refund Package" cuối cùng
let idxRefundPackage = _.findLastIndex(inv.ListTransAndFee, function(txn) {
    return txn.TransactionType == 'Other' 
        && txn.IsRefundTransaction 
        && txn.TransactionMethod == 'Package';
});

// Line 341: CHỈ mark isUpcharge = true nếu "Add fee" nằm SAU refund
if (inv.ListTransAndFee[i].type == 'Upcharge' 
    || (inv.ListTransAndFee[i].description == 'Add fee for paid by package' 
        && i > idxRefundPackage)) {  // ← ĐIỀU KIỆN NÀY!
    inv.ListTransAndFee[i].isUpcharge = true;
}
```

Từ screenshot của anh:
- `Transactions[0]`: "Add fee for paid by package 3" → **trước** refund → `isUpcharge = false` → UI ẩn
- `Transactions[3]`: "Add fee for paid by package" → **sau** refund → `isUpcharge = true` → UI hiện

**→ UI đúng.** Logic `i > idxRefundPackage` được implement từ PTE-3194, cố tình ẩn các "Add fee" cũ trước refund.

---

### Root Cause thực sự của bug PDF

**PDF generation** tại line 5755 **KHÔNG có** logic `idxRefundPackage`:

```javascript
// Line 5755 - PDF code - THIẾU check idxRefundPackage!
for (var jindex = 0; jindex < invoiceData.Transactions.length; jindex++) {
    if (invoiceData.Transactions[jindex].TransactionType == 'Upcharge' 
        || invoiceData.Transactions[jindex].Description == 'Add fee for paid by package') {
        // ↑ Render TẤT CẢ "Add fee" không phân biệt trước/sau refund
        htmlUpchargeFee += '<tr>...' + description + '...</tr>';
    }
}
```

**Kết quả:** PDF render cả 2, UI chỉ render 1 entry sau refund.

---

### Fix đúng chỗ — PDF generation (line 5754)

Fix phải thêm cùng logic `idxRefundPackage` vào PDF generator:

**File**: `v2_PatientInvoiceLogic.js` — line **5750-5771**

```javascript
if (invoiceData.Upcharge) {
    if (!invoiceData.Transactions) {
        invoiceData.Transactions = [];
    }
    // THÊM: tìm last refund package index (giống factory client)
    const idxRefundPkg = _.findLastIndex(invoiceData.Transactions, function(txn) {
        return txn.TransactionType == 'Other' 
            && txn.IsRefundTransaction 
            && txn.TransactionMethod == 'Package';
    });

    for (var jindex = 0; jindex < invoiceData.Transactions.length; jindex++) {
        const txn = invoiceData.Transactions[jindex];
        // THÊM điều kiện i > idxRefundPkg giống client
        const isUpcharge = txn.TransactionType == 'Upcharge'
            || (txn.Description == 'Add fee for paid by package' && jindex > idxRefundPkg);
        if (isUpcharge) {
            // ... render HTML
        }
    }
}
```

Anh muốn em apply fix này vào PDF code không?