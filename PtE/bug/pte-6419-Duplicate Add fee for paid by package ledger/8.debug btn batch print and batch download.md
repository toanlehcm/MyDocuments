Viewed v2_PatientInvoiceLogic.js:8401-8435

Để em đọc toàn bộ function `batchPrintPDFAllInvoicePatientWithTermATP` và các file liên quan trước.

Viewed v2_PatientInvoiceLogic.js:8408-8700

Tìm thấy hàm `transformInvoiceContentToTemplate` - đây là hàm được dùng chung. Để em đọc nó:

Ran command: `Select-String -Path "v2_PatientInvoiceLogic.js" -Pattern "function transformInvoiceContentToTemplate" | Select-Object -First 5`
Viewed v2_PatientInvoiceLogic.js:6889-7300
Viewed v2_PatientInvoiceLogic.js:7300-7500

**Tìm ra bug rồi!** Em đã có đủ bằng chứng. Bây giờ để em đọc thêm file client và action để hoàn chỉnh workflow trước khi viết report.

Ran command: `Select-String -Path "paymentPatientATP.js" -Pattern "batchPrintInvoices|batchDownloadInvoices|getBatchFilePDFInvoiceInfo|v4BatchPrintPDFInvoicePatientWithTermATP" | Select-Object -First 20`
Ran command: `Get-ChildItem -Recurse -Filter "paymentPatientATP.js" -Path "d:\Sources\pteverywhere\Client" | Select-Object FullName`
Ran command: `Select-String -Path "paymentPatientATP.js" -Pattern "batchPrintInvoices|batchDownloadInvoices|getBatchFilePDFInvoiceInfo|v4BatchPrintPDFInvoicePatientWithTermATP" | Select-Object LineNumber, Line`
Viewed paymentPatientATP.js:17316-18115

Đủ bằng chứng rồi. Em có tất cả thông tin cần thiết.

---

## 🔍 Kết quả điều tra — Bug PTE-6419 (Batch Print / Batch Download)

### Workflow của Batch Print & Batch Download

```
┌─────────────────────────────────────────────────

Để em viết report ngắn gọn, không lặp.

---

## 🔍 Debug PTE-6419 — Batch Print/Download

### Workflow Flow

```
[Client] batchPrintInvoices() / batchDownloadInvoices()
    → PopupBatchModeController (chọn date range / mode)
    → getBatchFilePDFInvoiceInfo(term)  [download]
    → api.call('v4BatchPrintPDFInvoicePatientWithTermATP', {...})
         ↓
[Server] v4BatchPrintPDFInvoicePatientWithTermATP (action)
         ↓
[Logic]  batchPrintPDFAllInvoicePatientWithTermATP
         ↓
[Shared] transformInvoiceContentToTemplate()  ← ⚠️ BUG HERE
```

---

### ✅ Bug Found — Root Cause

**File:** `v2_PatientInvoiceLogic.js`
**Function:** `transformInvoiceContentToTemplate()` (dùng chung cho cả batch print & batch download)
**Line:** **7399–7410**

```javascript
// Line 7399 — transformInvoiceContentToTemplate (shared function)
if(invoiceData.Upcharge && invoiceData.Upcharge > 0 ){
    if(!invoiceData.Transactions){
        invoiceData.Transactions = [];
    }
    for (var jindex = 0; jindex < invoiceData.Transactions.length; jindex++){
        // ❌ BUG: No idxRefundPkg filter — shows ALL "Add fee" entries
        if (invoiceData.Transactions[jindex].TransactionType == 'Upcharge'
            || invoiceData.Transactions[jindex].Description == 'Add fee for paid by package'){
            htmlUpchargeFee = htmlUpchargeFee + '<tr>...' + ...
        }
    }
}
```

---

### 📊 So sánh: Single Preview vs Batch

| | Single Invoice Preview | Batch Print/Download |
|---|---|---|
| **Function** | `printPDFInvoicePatientATP` (line ~5750) | `transformInvoiceContentToTemplate` (line 7399) |
| **Filter `idxRefundPkg`** | ✅ **Có** | ❌ **Thiếu** |
| **Chỉ show "Add fee" sau refund-pkg cuối** | ✅ Đúng | ❌ Show tất cả — bị duplicate |

---

### 📍 Root Cause Lines

| Dòng | Vấn đề |
|------|--------|
| **7399** | Condition thiếu `idxRefundPkg` filter |
| **7404** | So sánh `Description == 'Add fee for paid by package'` không có index guard |
| **7407** | `NewRemainAmount - RemainAmount` thiếu `|| 0` fallback |

---

**Tóm lại:** `transformInvoiceContentToTemplate` — hàm shared dùng cho batch — **thiếu hoàn toàn logic `idxRefundPkg`** đã được fix trong single-preview. Đây là root cause của duplicate "Add fee" trong batch PDF.

Anh muốn em fix luôn không?