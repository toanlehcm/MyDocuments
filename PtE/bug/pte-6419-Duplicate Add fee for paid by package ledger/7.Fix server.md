Viewed paymentFactoryPtE.js:272-319

Xem lại code hiện tại của PDF generation để apply đúng:

Viewed v2_PatientInvoiceLogic.js:5748-5775
Edited v2_PatientInvoiceLogic.js

Verify lại code sau khi apply:

Viewed v2_PatientInvoiceLogic.js:5750-5782

✅ Fix đã apply thành công. Đây là tóm tắt:

---

### Đã làm gì

**File**: `v2_PatientInvoiceLogic.js` — PDF generation (line 5754-5761)

**Thay đổi:**
- ❌ Xóa `console.log` debug tạm thời
- ✅ Thêm `idxRefundPkg` — tìm index của transaction "Refund Package" cuối cùng
- ✅ Thêm điều kiện `jindex > idxRefundPkg` — chỉ render "Add fee" entries **sau** refund (giống hệt logic client PTE-3194)

**Before:**
```javascript
if (txn.TransactionType == 'Upcharge' 
    || txn.Description == 'Add fee for paid by package') { // ← render ALL
```

**After:**
```javascript
const isPackageFee = txn.Description == 'Add fee for paid by package' 
    && jindex > idxRefundPkg  // ← chỉ render entry SAU refund
if (txn.TransactionType == 'Upcharge' || isPackageFee) {
```

---

### Tại sao fix đúng

| | UI (paymentFactoryPtE) | PDF (trước fix) | PDF (sau fix) |
|--|--|--|--|
| Logic | `i > idxRefundPackage` | Không có | `jindex > idxRefundPkg` ✅ |
| Kết quả | Hiện 1 dòng ✅ | Hiện 2 dòng ❌ | Hiện 1 dòng ✅ |

Anh restart server và test lại theo steps của bug PTE-6419 nhé!